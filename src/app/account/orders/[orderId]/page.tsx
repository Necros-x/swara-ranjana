import type { Metadata } from "next";\nimport Link from "next/link";\nimport { notFound } from "next/navigation";\nimport { getCustomerTicketBundle } from "@/lib/account/data";\nimport { requireCustomer } from "@/lib/auth/requireCustomer";\nimport AccountTicketViewer from "@/public-site/components/account/AccountTicketViewer";\nexport const dynamic="force-dynamic"; export const revalidate=0;\nexport const metadata:Metadata={title:"Ticket — Swara Ranjana",robots:{index:false,follow:false,nocache:true},referrer:"no-referrer"};\nexport default async function CustomerOrderPage({params}:{params:Promise<{orderId:string}>}){const {orderId}=await params;const {supabase,customer}=await requireCustomer();const bundle=await getCustomerTicketBundle(supabase,customer,orderId);if(!bundle)notFound();return <main className="min-h-screen bg-[#F7F9FA] px-4 py-10 text-[#0E1721] sm:py-14"><div className="mx-auto max-w-5xl"><div className="mb-7 flex justify-between"><Link href="/account" className="text-xs uppercase tracking-[.18em] text-[#7D8A95] hover:text-[#2271B1]">← My account</Link><span className="text-xs text-[#7D8A95]">{bundle.eventName}</span></div><AccountTicketViewer bundle={bundle}/></div></main>;}\n